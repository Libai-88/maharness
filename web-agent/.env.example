# ============================================
# Web Agent 环境配置模板（复制为 .env 后填写）
# 支持任意多个 OpenAI 兼容 Provider，网页端可切换
# ============================================

# ---- DeepSeek ----
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-chat

# ---- OpenAI ----
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# ---- 阿里通义千问（示例）----
# DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
# DASHSCOPE_API_KEY=
# DASHSCOPE_MODEL=qwen-plus

# ---- 可选：语义缓存 Embedding（不配置则 L1 缓存自动跳过）----
# EMBEDDING_BASE_URL=https://api.deepseek.com/v1
# EMBEDDING_API_KEY=
# EMBEDDING_MODEL=deepseek-embedding

# ---- 可选：联网搜索（Tavily，免费额度；不配置则自动降级 DuckDuckGo）----
# TAVILY_API_KEY=
# 可选：搜索请求走 HTTP 代理（如本地代理 http://127.0.0.1:7897），网络受限环境使用
# SEARCH_PROXY=

# ---- 服务端口 ----
PORT=3000

# ---- 沙箱根目录（文件工具可访问范围；默认当前工作区根目录）----
# SANDBOX_ROOT=D:\DEEPSEEK
